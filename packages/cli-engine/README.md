# CLI Engine

Runs the local `/prd` workflow from a terminal-friendly `prd` command.

## Commands

```bash
npm run prd -w @katalyst/cli-engine -- "Describe the product"
```

## Output

Generated PRDs are written to `.katalyst/generated/prd.md` in the current project.
